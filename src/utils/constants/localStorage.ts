const LOCAL_STORAGE_PREFIX = 'java-study-platform'

export const LOCAL_STORAGE = {
  USER: `${LOCAL_STORAGE_PREFIX}-user`,
  SESSION: `${LOCAL_STORAGE_PREFIX}-session`,
  IS_AUTH: `${LOCAL_STORAGE_PREFIX}-is-auth`
}
